import{_ as e,c as t,o,a as s}from"./app.3b6acc86.js";const b=JSON.parse('{"title":"Webapp Output Target: www","description":"","frontmatter":{},"headers":[{"level":2,"title":"Config","slug":"config"}],"relativePath":"guide/www.md"}'),d={name:"guide/www.md"},r=s(`<h1 id="webapp-output-target-www" tabindex="-1">Webapp Output Target: <code>www</code> <a class="header-anchor" href="#webapp-output-target-www" aria-hidden="true">#</a></h1><p>The <code>www</code> output target type is oriented for webapps and websites, hosted from an http server, which can benefit from prerendering and service workers, such as this very site you&#39;re reading. If the <code>outputTarget</code> config is not provided it&#39;ll default to having just the <code>www</code> type.</p><p>Even if a project is meant to only build a reusable component library, the <code>www</code> output target is useful to build out and test the components throughout development.</p><div class="language-tsx"><span class="copy"></span><pre><code><span class="line"><span style="color:#FFCB6B;">outputTargets</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> [</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#F07178;">type</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">www</span><span style="color:#89DDFF;">&#39;</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#A6ACCD;">]</span></span>
<span class="line"></span></code></pre></div><h2 id="config" tabindex="-1">Config <a class="header-anchor" href="#config" aria-hidden="true">#</a></h2><table><thead><tr><th>Property</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>baseUrl</code></td><td>The <code>baseUrl</code> represents the site&#39;s &quot;base&quot; url to be served from. For example, this site&#39;s base url is <code>https://rindojs.web.app/</code>. However, if the entire site&#39;s output is to live within a sub directory, then this directory&#39;s path should be the <code>baseUrl</code>. For example, Navify&#39;s documentation is a stand-alone Rindo site that lives in the <code>/docs</code> directory within <code>https://navifyframework.web.app/</code>. In this example, <code>https://navifyframework.web.app/docs/</code> would be the base url.</td><td><code>/</code></td></tr><tr><td><code>buildDir</code></td><td>The <code>buildDir</code> is the directory of Rindo&#39;s generated scripts, such as the component files. For production builds, this directory will contain both <code>es5</code> and <code>esm</code> builds for each component. (Don&#39;t worry, users only request the one their browser needs.)</td><td><code>build</code></td></tr><tr><td><code>dir</code></td><td>The <code>dir</code> config specifies the public web distribution directory. This directory is commonly the root directory of an app to be served, such as serving static files from. This directory is built and rebuilt directly from the source files. Additionally, since this is a build target, all files will be deleted and rebuilt after each build, so it&#39;s best to always copy source files into this directory. It&#39;s recommended this directory is not committed to a repository.</td><td><code>www</code></td></tr><tr><td><code>empty</code></td><td>By default, before each build the <code>dir</code> directory will be emptied of all files. However, to prevent this directory from being emptied change this value to <code>false</code>.</td><td><code>true</code></td></tr><tr><td><code>indexHtml</code></td><td>The <code>indexHtml</code> property represents the location of the root index html file.</td><td><code>index.html</code></td></tr><tr><td><code>serviceWorker</code></td><td>The <code>serviceWorker</code> config lets you customize the service worker that gets automatically generated by the Rindo compiler. To override Rindo&#39;s defaults, set any of the values listed in the <a href="https://developers.google.com/web/tools/workbox/modules/workbox-build#full_generatesw_config" target="_blank" rel="noopener noreferrer">Workbox documentation</a>.</td><td></td></tr></tbody></table>`,6),a=[r];function i(c,n,l,p,h,u){return o(),t("div",null,a)}const f=e(d,[["render",i]]);export{b as __pageData,f as default};
